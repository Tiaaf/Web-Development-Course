<!DOCTYPE html>
<html>
  <head>
    <link type="text/css" rel="stylesheet" href="/static/main.css">
    <title>Blog</title>
  </head>
  

  <body>
    <h1>My Blog</h1>

    <hr>
     {% for text in texts %}
      <div class="text">
        <div class="text-title">{{text.title}} &nbsp;  {{text.created.strftime("%b %d, %Y")}}</div>
        <pre class="text-body">{{text.text}}</pre>
      </div>
     {% endfor %}
        
  </body>
</html>
