<!DOCTYPE html>

<html>
  <head>
    <title>Blog</title>
  </head>
  
    <style type="text/css">
      body {
      font-family: sans-serif;
      width: 800 px;
      margin: 0 auto;
      padding: 10px;
      }

      hr {
      margin: 20px auto;
      }
      
      .text-title {
      font-weight:bold;
      font-size: 20px;
      }

      .text-body {
      margin: 0;
      font-size: 17px;
      }
    </style>

  <body>
    <h1>My Blog</h1>

    <hr>
     {% for text in texts %}
      <div class="text">
        <div class="text-title">{{text.title}} &nbsp;  {{text.created}}</div>
        <pre class="text-body">{{text.text}}</pre>
      </div>
     {% endfor %}
        
  </body>
</html>
